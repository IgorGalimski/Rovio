public struct PlayerModel
{
    public float RotationSpeed;
    public float MovementSpeed;

    public PlayerModel(float rotationSpeed, float movementSpeed)
    {
        RotationSpeed = rotationSpeed;
        MovementSpeed = movementSpeed;
    }
}